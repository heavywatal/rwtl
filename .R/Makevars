CFLAGS = -Wall -g -O2 -march=native -mtune=native
CXXFLAGS = $(CFLAGS)
CXX11FLAGS = $(CFLAGS)
CXX14FLAGS = $(CFLAGS)
CXX17FLAGS = $(CFLAGS)
LDFLAGS = -L${HOME}/local/lib -L${HOME}/local/boost/lib -L${HOME}/.linuxbrew/lib -L${HOME}/.homebrew/lib  -L/usr/local/lib
